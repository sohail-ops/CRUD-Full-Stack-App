export const environment = {
  production: false,
  apiIp: 'localhost:8080'
};

/*
 * Enter "Public-IP-address" of your VM in the "apiIp" field where you hosted Frontend, or 
 * "localhost:8080" if you are running backend on local machine.
 */